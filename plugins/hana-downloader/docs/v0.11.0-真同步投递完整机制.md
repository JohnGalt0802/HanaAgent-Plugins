# v0.11.0 真同步投递完整机制

> 落盘时间：2026-09-02
> 版本：hana-downloader v0.11.0（v1 插件契约 + 真同步投递 + bundle 魔改）
> 状态：完整修复链 4 commit 入库 + probe-final9 agent 实测验证通过

---

## 0. 一句话总结

下载完成时，**未收束会话**通过 host `agent/pre-step` 注入 HBR 到下一条 LLM API 请求的 `payload.messages`，agent 在**同回合**感知（真同步）；**已收束会话**走 `deferred:resolve` 异步 triggerTurn 唤醒。

---

## 1. 三条投递路径

```
下载 settle (done/cancelled/error/stall)
   │
   ▼
delivery.handleFinal(task) / handleStall(task)
   │
   ├─ 静默分支：consumedByWait / canceledBy=agent → 直接 markDelivered + finalizeRegistry
   │
   ├─ tailSettled(jsonl) 判会话状态
   │
   ├─ true (已收束) ────────────────────────────→ deliverAsync
   │                                                  │
   │                                                  └─ bus.request("deferred:resolve")
   │                                                       └─ host deliverCustomMessage
   │                                                            ├─ isStreaming=true → followUp 入队
   │                                                            └─ isStreaming=false → triggerTurn 立即开新 turn
   │
   └─ false (未收束) ────────────────────────────→ enqueueSync
                                                       │
                                                       ├─ pending Map (key=sessionFile::taskId)
                                                       ├─ 30 秒 timer (SYNC_WAIT_MS=30000)
                                                       │
                          ┌────────────────────────────┴────────────────────────────┐
                          │                                                         │
                  injectForSession 消费 (期 30s)                          timer 触发 (30s 超时)
                          │                                                         │
                          ▼                                                         ▼
                  真同步：HBR 推 payload.messages                    异步兜底：deliverAsync
                  → agent 当前轮 LLM API messages 含 HBR            → followUp / triggerTurn
                  → agent 同回合感知                                          → 下一轮 input / 新 turn 感知
```

---

## 2. 真同步注入的 host 机制

### 2.1 host bundle 魔改点（必要前提）

宿主 0.814.0 bundle 不暴露 `sessionHooks` registry，v1 插件契约无法注册 adjudicator。**bundle 必须魔改一行**：

```js
// bundle/index.js:171120
globalThis.__sessionHooks = j;   // 暴露 hooks registry 给插件
```

- **备份**：`D:\HanakoWorks\_temp\bundle-index.js.0.814.0-魔改前.bak`
- **恢复**：从此备份覆盖回 bundle/index.js 即可
- **运行时配置**：不进 git（每个宿主升级需重新魔改）

### 2.2 plugin index.js onload 注册 adjudicator

```js
// extensions/index.js (lib/index.js bundled)
async function onload() {
  const hooks = globalThis.__sessionHooks;   // 魔改点暴露的 registry
  if (!hooks || typeof hooks.onDecision !== "function") {
    dbgLog(`onload: sessionHooks unavailable`);
    return;
  }
  hooks.onDecision("agent/pre-step", injectVersion, {
    owner: "hana-downloader",
    order: 999,
  });
  dbgLog(`registered agent/pre-step injection adjudicator`);
}
```

`injectVersion` 接收 `{ session, messages }`，调用 `delivery.injectForSession(session.sessionFile, { messages })`，把 pending 中的 HBR 推到 `payload.messages` 并 return 出去。

### 2.3 host decide waterfall 调用链

```
LLM API 请求前 → host decide("agent/pre-step")
   │
   ├─ host 内置 hooks (order 0/10/20) — 媒体处理 / 压缩
   │
   └─ 插件注入版 (order 999) — delivery.injectForSession
         │
         └─ payload.messages.push({ role: "user", content: entry.content, details })
            │
            └─ host spread 到 invocation
                  │
                  └─ agent 下一条 LLM API 请求的 messages 数组里看到 HBR
```

---

## 3. 关键代码位置（dev 源 / community 源都同步）

| 文件 | 行 | 作用 |
|---|---|---|
| `lib/delivery.js:18` | `TERMINAL` Set | 终态判断（done/failed/canceled/interrupted）|
| `lib/delivery.js:19` | `SYNC_WAIT_MS = 30000` | 同步等待窗口（典型 agent reasoning + tool loop 覆盖）|
| `lib/delivery.js:23-30` | `statusOf(task)` | 取 task.state 或 task.status |
| `lib/delivery.js:29-50` | `tailSettled()` | 看最近 assistant 的 stopReason（**非**倒序找第一个 assistant+stop）|
| `lib/delivery.js:114-130` | `buildEntry()` | **7 属性版本**：status + event-status + source + plugin + type + action + task-id |
| `lib/delivery.js:131-150` | `deliverAsync()` | bus.request("deferred:resolve") |
| `lib/delivery.js:151-185` | `enqueueSync()` | pending Map + 30 秒 timer |
| `lib/delivery.js:253-273` | `injectForSession()` | 推 HBR 到 payload.messages（写 inject-content.log）|
| `lib/index.js onload` | `agent/pre-step` adjudicator 注册（order 999）| 真同步注入入口 |
| `lib/dlcore.js:185-190` | 错误 catch 块 | **移除 `_fireFinal`**，由外层 finally 统一触发 |
| `lib/dlcore.js:591-595` | `_run() finally` | `_fireFinal(task)` 统一终态触发 |

---

## 4. 4 个 commit 修复链（按时间序）

| Commit | 标题 | 关键修复 |
|---|---|---|
| `200dbf03` | 真同步注入 v0.11.0 完整版 | index.js onload agent/pre-step adjudicator（order 999）+ dl-sync.js provider/before-request（v1 不被调度，后删）|
| `6ed9028f` | 真同步注入修复链 | 移除 DIAG（order 998 挡住 waterfall）+ 修复 tailSettled bug + SYNC_WAIT_MS 2500→30000 + 删除 dl-sync.js |
| `fadbdf47` | 诊断 dbgLog | injectForSession 写 `D:\HanakoWorks\_temp\inject-content.log`（看实际注入的 content 头部）|
| `8ed85699` | 恢复双 status + 修 dlcore fireFinal | buildEntry 恢复 7 属性（含 event-status/source/plugin/action）+ dlcore 错误路径移除 fireFinal（避免覆盖成功路径）|

---

## 5. 三种时机的精确行为

| 阶段 | agent 状态 | isStreaming | injectForSession | timer 触发后路径 | agent 感知时机 |
|---|---|---|---|---|---|
| **A** reasoning 中调工具 | toolUse | true |推 HBR 进 payload.messages | injectForSession 已消费 | **当前轮** LLM API messages 含 HBR ✓ |
| **B** 输出 stop token 中 | stream中 | true | 不调（host 已不调 agent/pre-step）| followUp 入队 | **下一轮** input（被 followUp 消费）|
| **C** stop 已发 + host 处理中 | 处理中 | false | 不调 | triggerTurn 立即开新 turn | **新轮** input |
| **D** pre_reply_interlude | 渲染最终回答 | false | 不调 | triggerTurn | **新轮** input |
| **E** 用户已收到最终回答 | 静默等用户 | false | 不调 | triggerTurn | **新轮** input |

**核心洞察**：
- A 阶段真同步生效（agent 当前轮 API messages 看到 HBR）
- B-E 阶段真同步必然失效（host 不再调 agent/pre-step），但 timer 触发后 host triggerTurn **主动开新 turn**让 agent 看到 HBR（不等用户输入）
- **"等不到"实际不存在**——host triggerTurn 是 host 内部驱动，不依赖用户输入

---

## 6. buildEntry 双 status 字段设计

```js
function buildEntry(taskId, result) {
  // status（legacyStatus）= success/failed/aborted，宿主 interlude / 前端 detail 兼容
  // event-status（新语义）= done/cancelled/error，agent 应该看这个
  const eventStatus = result.status || (result.state === "done" ? "done" : result.state === "canceled" ? "cancelled" : "error");
  const legacyStatus = eventStatus === "done" ? "success" : eventStatus === "cancelled" ? "aborted" : "failed";
  const action = eventStatus === "done" || eventStatus === "cancelled" ? "none" : "decide";
  const body = JSON.stringify(result, null, 2);
  return {
    customType: "hana-background-result",
    content: `<hana-background-result task-id="${esc(taskId)}" status="${esc(legacyStatus)}" event-status="${esc(eventStatus)}" source="system" plugin="hana-downloader" type="download" action="${esc(action)}">\n${esc(body)}\n</hana-background-result>`,
    display: false,
    details: { schemaVersion: 2, taskId, deliveryId: `sync:${taskId}:${Date.now()}`, eventStatus, action },
  };
}
```

**HBR 根标签完整属性**：task-id / status / event-status / source / plugin / type / action

agent 判断时**优先看 event-status**（done/cancelled/error），status 仅用于兼容宿主/前端展示。

---

## 7. 验证证据（probe-final9 / 2026-09-02）

`inject-content.log`（`D:\HanakoWorks\_temp\inject-content.log`）：
```
[2026-09-02T03:16:33.876Z] key=e701d3cd-mtjiya4w entry.content_head=<hana-background-result task-id="e701d3cd-mtjiya4w" status="success" event-status="done" source="system" plugin="hana-downloader" type="download" action="none">
{
  "taskId": "e701d3cd-mtjiya4w",
  ...
```

agent 03:16:39 实测 thinking（session sess_0mtjioehz）：
```
HBR 根标签属性：
- task-id=e701d3cd-mtjiya4w
- status="success"
- event-status="done"
- source="system"
- plugin="hana-downloader"
- type="download"
- action="none"

status 和 event-status 分别是什么？status="success"，event-status="done"。
这两个不是矛盾关系——它们是两个不同维度的状态。

下载状态：success，文件大小：262144 bytes（256 KB）
```

stall-debug.log：
```
[2026-09-02T03:08:53.155Z] DBG registered agent/pre-step injection adjudicator (owner=hana-downloader, order=999)
[2026-09-02T03:14:31.724Z] DBG agent/pre-step adjudicator called | sessionFile=... msgs=395 delivery=object
[stall] injected msgs 10->11
```

---

## 8. 调试手段

| 日志文件 | 内容 | 用途 |
|---|---|---|
| `D:\HanakoWorks\_temp\inject-content.log` | injectForSession 实际 push 的 content 头部（前 220 字符）| 确认注入版是否被调 + 实际 payload 内容 |
| `D:\HanakoWorks\_temp\inject-debug.log` | injectForSession 入口 + pending.size + 匹配结果 | 确认 pending 是否有数据 + 是否真匹配 sessionFile |
| `D:\HanakoWorks\_temp\stall-debug.log` | onload / adjudicator called / injected msgs 计数 | 完整链路诊断 |

dbgLog 注入代码（lib/delivery.js injectForSession 内）保留为诊断开关，运行时写日志——可调试但无副作用。

---

## 9. 部署说明

### 9.1 plugin 加载

- dev 源：`D:\HanakoWorks\hana-downloader`（git 主仓）
- community 源：`C:\Users\John Galt\.hanako\plugins\hana-downloader`（插件加载目录）
- 两份需同步：`Copy-Item dev/* community/* -Recurse -Force`

### 9.2 bundle 魔改（必要）

宿主升级或重装后必须重做魔改：

```powershell
# 1. 备份
Copy-Item "$env:USERPROFILE\.hanako\artifacts\server\0.814.0-win32-x64\bundle\index.js" `
         "D:\HanakoWorks\_temp\bundle-index.js.0.814.0-魔改前.bak" -Force

# 2. 在 bundle/index.js 第 171120 行附近加：
#    globalThis.__sessionHooks = j;

# 3. 重启宿主
pwsh -File D:\HanakoWorks\_tools\restart-hana\restart-hana-reliable.ps1
```

**魔改点必须**——宿主内置 session-hooks registry 不暴露，注入版无法注册。

### 9.3 真同步开关

无显式开关——plugin 加载时通过 `globalThis.__sessionHooks.onDecision("agent/pre-step", ...)` 自动注册。如宿主未魔改，onload 静默 return（不抛错），降级为纯异步 triggerTurn 路径。

---

## 10. 已知边界与后续改进

- **bundle 魔改是侵入式**——宿主升级会失效，需重做。理想路径：宿主原生暴露 sessionHooks（dsh 已向宿主团队建议）
- **30 秒覆盖典型场景**——长 reasoning（>30s）走 followUp 兜底，agent 多等一回合但 HBR 不丢
- **HBR 跨多轮保留**——enqueueSync 进 pending 后即使跨多 turn 仍能匹配同一 sessionFile 的 agent/pre-step 调用
- **dlcore fireFinal 已修复**——错误 catch 块移除 fireFinal，统一由 _run() finally 触发（避免双触发覆盖）
- **后续**：宿主 bundle 修复后建议移除 bundle 魔改（dsh 文档 §5.2 双 status 设计建议已在合并中）